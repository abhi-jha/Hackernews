
sudo pip3 install pystan
sudo apt-get install libmysqlclient-dev
sudo pip3 install mysql-python
sudo pip3 install mysqlclient
sudo apt-get install python-dev
sudo apt-get install libsqlite3-dev
sudo pip3 install peewee

#Execute the script:
#python3 hacknernews.py

#After execution, make sure the initial schema exists for the data to be imported/exported:

#Export:

mysqldump -u username -ppassword hn > db_backup.sql

#Import:

#mysql -u username -ppassword hn < db_backup.sql
